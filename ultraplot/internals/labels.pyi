# @generated by tools/generate_stubs.py; do not edit
# fmt: off
"""
Utilities related to matplotlib text labels.
"""
from _typeshed import Incomplete
import matplotlib.patheffects as mpatheffects
import matplotlib.text as mtext
from matplotlib.font_manager import FontProperties
from ..config import rc
from . import ic
LABEL_PSEUDO_PROPS = frozenset({'border', 'bordercolor', 'borderinvert', 'borderwidth', 'borderstyle', 'bbox', 'bboxcolor', 'bboxstyle', 'bboxalpha', 'bboxpad'})

def _split_label_props(kwargs: Incomplete) -> Incomplete:
    """Split a kwargs dict into (label_props, text_kwargs) so the latter can be
passed to `mtext.Text(...)` and the former applied via `_update_label`."""
    ...

def merge_font_properties(dest_fp: FontProperties, src_fp: FontProperties) -> FontProperties:
    ...

def _transfer_label(src: mtext.Text, dest: mtext.Text) -> None:
    """Transfer the input text object properties and content to the destination
text object. Then clear the input object text."""
    ...

def _update_label(text: Incomplete, props: Incomplete=None, **kwargs: Incomplete) -> Incomplete:
    """Add a monkey patch for ``Text.update`` with pseudo "border" and "bbox"
properties without wrapping the entire class. This facillitates inset titles."""
    ...
