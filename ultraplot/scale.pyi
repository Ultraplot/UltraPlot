# @generated by tools/generate_stubs.py; do not edit
# fmt: off
"""
Various axis [ScaleBase](https://matplotlib.org/stable/api/_as_gen/matplotlib.scale.ScaleBase.html) classes.
"""
from _typeshed import Incomplete
import copy
import matplotlib.scale as mscale
import matplotlib.ticker as mticker
import matplotlib.transforms as mtransforms
import numpy as np
import numpy.ma as ma
from . import ticker as pticker
from .internals import _not_none, _version_mpl, ic, warnings
__all__ = ['CutoffScale', 'ExpScale', 'FuncScale', 'InverseScale', 'LinearScale', 'LogitScale', 'LogScale', 'MercatorLatitudeScale', 'PowerScale', 'SineLatitudeScale', 'SymmetricalLogScale']

def _parse_logscale_args(*keys: Incomplete, **kwargs: Incomplete) -> Incomplete:
    """Parse arguments for `LogScale` and `SymmetricalLogScale` that
inexplicably require `x` and `y` suffixes by default. Also
change the default `linthresh` to ``1``."""
    ...

class _Scale(object):
    """Mix-in class that standardizes the behavior of
[set_default_locators_and_formatters](https://matplotlib.org/stable/api/_as_gen/matplotlib.scale.ScaleBase.set_default_locators_and_formatters.html)
and [get_transform](https://matplotlib.org/stable/api/_as_gen/matplotlib.scale.ScaleBase.get_transform.html). Also overrides
`__init__` so you no longer have to instantiate scales with an
[Axis](https://matplotlib.org/stable/api/_as_gen/matplotlib.axis.Axis.html) instance."""

    def __init__(self, *args: Incomplete, **kwargs: Incomplete) -> None:
        ...

    def set_default_locators_and_formatters(self, axis: Incomplete, only_if_default: Incomplete=False) -> Incomplete:
        """Apply all locators and formatters defined as attributes on
initialization and define defaults for all scales.

Parameters
----------
axis : [Axis](https://matplotlib.org/stable/api/_as_gen/matplotlib.axis.Axis.html)
    The axis.
only_if_default : bool, optional
    Whether to refrain from updating the locators and formatters if the
    axis is currently using non-default versions. Useful if we want to
    avoid overwriting user customization when the scale is changed."""
        ...

    def get_transform(self) -> Incomplete:
        """Return the scale transform."""
        ...

class LinearScale(_Scale, mscale.LinearScale):
    """As with [LinearScale](https://matplotlib.org/stable/api/_as_gen/matplotlib.scale.LinearScale.html) but with
[AutoFormatter](https://ultraplot.readthedocs.io/en/stable/api/ultraplot.ticker.AutoFormatter.html) as the default major formatter."""
    name = 'linear'

    def __init__(self, **kwargs: Incomplete) -> None:
        """See also
--------
ultraplot.constructor.Scale"""
        ...

class LogitScale(_Scale, mscale.LogitScale):
    """As with [LogitScale](https://matplotlib.org/stable/api/_as_gen/matplotlib.scale.LogitScale.html) but with [AutoFormatter](https://ultraplot.readthedocs.io/en/stable/api/ultraplot.ticker.AutoFormatter.html)
as the default major formatter."""
    name = 'logit'

    def __init__(self, **kwargs: Incomplete) -> None:
        """Parameters
----------
nonpos : {'mask', 'clip'}
  Values outside of (0, 1) can be masked as invalid, or clipped to a
  number very close to 0 or 1.

See also
--------
ultraplot.constructor.Scale"""
        ...

class LogScale(_Scale, mscale.LogScale):
    """As with [LogScale](https://matplotlib.org/stable/api/_as_gen/matplotlib.scale.LogScale.html) but with [AutoFormatter](https://ultraplot.readthedocs.io/en/stable/api/ultraplot.ticker.AutoFormatter.html)
as the default major formatter. `x` and `y` versions of each keyword
argument are no longer required."""
    name = 'log'

    def __init__(self, **kwargs: Incomplete) -> None:
        """Parameters
----------
base : float, default: 10
    The base of the logarithm.
nonpos : {'mask', 'clip'}, optional
    Non-positive values in *x* or *y* can be masked as
    invalid, or clipped to a very small positive number.
subs : sequence of int, default: ``[1 2 3 4 5 6 7 8 9]``
    Default *minor* tick locations are on these multiples of each power
    of the base. For example, ``subs=(1, 2, 5)`` draws ticks on 1, 2,
    5, 10, 20, 50, 100, 200, 500, etc.
basex, basey, nonposx, nonposy, subsx, subsy
    Aliases for the above keywords. These used to be conditional
    on the *name* of the axis.

See also
--------
ultraplot.constructor.Scale"""
        ...

class SymmetricalLogScale(_Scale, mscale.SymmetricalLogScale):
    """As with [SymmetricalLogScale](https://matplotlib.org/stable/api/_as_gen/matplotlib.scale.SymmetricalLogScale.html) but with
[AutoFormatter](https://ultraplot.readthedocs.io/en/stable/api/ultraplot.ticker.AutoFormatter.html) as the default major formatter.
`x` and `y` versions of each keyword argument are no longer
required."""
    name = 'symlog'

    def __init__(self, **kwargs: Incomplete) -> None:
        """Parameters
----------
base : float, default: 10
    The base of the logarithm.
linthresh : float, default: 1
    Defines the range ``(-linthresh, linthresh)``, within which the plot
    is linear.  This avoids having the plot go to infinity around zero.
linscale : float, default: 1
    This allows the linear range ``(-linthresh, linthresh)`` to be
    stretched relative to the logarithmic range. Its value is the
    number of decades to use for each half of the linear range. For
    example, when `linscale` is ``1`` (the default), the space used
    for the positive and negative halves of the linear range will be
    equal to one decade in the logarithmic range.
subs : sequence of int, default: ``[1 2 3 4 5 6 7 8 9]``
    Default *minor* tick locations are on these multiples of each power
    of the base. For example, ``subs=(1, 2, 5)`` draws ticks on 1, 2,
    5, 10, 20, 50, 100, 200, 500, etc.
basex, basey, linthreshx, linthreshy, linscalex, linscaley, subsx, subsy
    Aliases for the above keywords. These keywords used to be
    conditional on the name of the axis.

See also
--------
ultraplot.constructor.Scale"""
        ...

class FuncScale(_Scale, mscale.ScaleBase):
    """Axis scale composed of arbitrary forward and inverse transformations."""
    name = 'function'

    def __init__(self, transform: Incomplete=None, invert: Incomplete=False, parent_scale: Incomplete=None, **kwargs: Incomplete) -> None:
        """Parameters
----------
- `transform`: The transform used to translate units from the parent axis to the secondary axis.
- `invert`: If ``True``, the forward and inverse functions are *swapped*.
- `parent_scale`: The axis scale of the "parent" axis.
- `major_locator, minor_locator`: The default major and minor locator.
- `major_formatter, minor_formatter`: The default major and minor formatter.

[Full API documentation](https://ultraplot.readthedocs.io/en/stable/api/ultraplot.scale.FuncScale.html)"""
        ...

class FuncTransform(mtransforms.Transform):
    input_dims = 1
    output_dims = 1
    is_separable = True
    has_inverse = True

    def __init__(self, forward: Incomplete, inverse: Incomplete) -> None:
        """Parameters
----------
shorthand_name : str
    A string representing the "name" of the transform. The name carries
    no significance other than to improve the readability of
    ``str(transform)`` when DEBUG=True."""
        ...

    def inverted(self) -> Incomplete:
        """Return the corresponding inverse transformation.

It holds ``x == self.inverted().transform(self.transform(x))``.

The return value of this method should be treated as
temporary.  An update to *self* does not cause a corresponding
update to its inverted copy."""
        ...

    def transform_non_affine(self, values: Incomplete) -> Incomplete:
        """Apply only the non-affine part of this transformation.

``transform(values)`` is always equivalent to
``transform_affine(transform_non_affine(values))``.

In non-affine transformations, this is generally equivalent to
``transform(values)``.  In affine transformations, this is
always a no-op.

Parameters
----------
values : array
    The input values as an array of length `input_dims` or
    shape (N, `input_dims`).

Returns
-------
array
    The output values as an array of length `output_dims` or
    shape (N, `output_dims`), depending on the input."""
        ...

class PowerScale(_Scale, mscale.ScaleBase):
    """"Power scale" that performs the transformation

.. math::

    x^{c}"""
    name = 'power'

    def __init__(self, power: Incomplete=1, inverse: Incomplete=False) -> None:
        """Parameters
----------
power : float, optional
    The power :math:`c` to which :math:`x` is raised.
inverse : bool, optional
    If ``True`` this performs the inverse operation :math:`x^{1/c}`."""
        ...

    def limit_range_for_scale(self, vmin: Incomplete, vmax: Incomplete, minpos: Incomplete) -> Incomplete:
        """Return the range *vmin* and *vmax* limited to positive numbers."""
        ...

class PowerTransform(mtransforms.Transform):
    input_dims = 1
    output_dims = 1
    has_inverse = True
    is_separable = True

    def __init__(self, power: Incomplete) -> None:
        """Parameters
----------
shorthand_name : str
    A string representing the "name" of the transform. The name carries
    no significance other than to improve the readability of
    ``str(transform)`` when DEBUG=True."""
        ...

    def inverted(self) -> Incomplete:
        """Return the corresponding inverse transformation.

It holds ``x == self.inverted().transform(self.transform(x))``.

The return value of this method should be treated as
temporary.  An update to *self* does not cause a corresponding
update to its inverted copy."""
        ...

    def transform_non_affine(self, a: Incomplete) -> Incomplete:
        """Apply only the non-affine part of this transformation.

``transform(values)`` is always equivalent to
``transform_affine(transform_non_affine(values))``.

In non-affine transformations, this is generally equivalent to
``transform(values)``.  In affine transformations, this is
always a no-op.

Parameters
----------
values : array
    The input values as an array of length `input_dims` or
    shape (N, `input_dims`).

Returns
-------
array
    The output values as an array of length `output_dims` or
    shape (N, `output_dims`), depending on the input."""
        ...

class InvertedPowerTransform(mtransforms.Transform):
    input_dims = 1
    output_dims = 1
    has_inverse = True
    is_separable = True

    def __init__(self, power: Incomplete) -> None:
        """Parameters
----------
shorthand_name : str
    A string representing the "name" of the transform. The name carries
    no significance other than to improve the readability of
    ``str(transform)`` when DEBUG=True."""
        ...

    def inverted(self) -> Incomplete:
        """Return the corresponding inverse transformation.

It holds ``x == self.inverted().transform(self.transform(x))``.

The return value of this method should be treated as
temporary.  An update to *self* does not cause a corresponding
update to its inverted copy."""
        ...

    def transform_non_affine(self, a: Incomplete) -> Incomplete:
        """Apply only the non-affine part of this transformation.

``transform(values)`` is always equivalent to
``transform_affine(transform_non_affine(values))``.

In non-affine transformations, this is generally equivalent to
``transform(values)``.  In affine transformations, this is
always a no-op.

Parameters
----------
values : array
    The input values as an array of length `input_dims` or
    shape (N, `input_dims`).

Returns
-------
array
    The output values as an array of length `output_dims` or
    shape (N, `output_dims`), depending on the input."""
        ...

class ExpScale(_Scale, mscale.ScaleBase):
    """"Exponential scale" that performs either of two transformations. When
`inverse` is ``False`` (the default), performs the transformation

.. math::

    Ca^{bx}

where the constants :math:`a`, :math:`b`, and :math:`C` are set by the
input (see below). When `inverse` is ``True``, this performs the inverse
transformation

.. math::

    (\\log_a(x) - \\log_a(C))/b

which in appearance is equivalent to `LogScale` since it is just a linear
transformation of the logarithm."""
    name = 'exp'

    def __init__(self, a: Incomplete=np.e, b: Incomplete=1, c: Incomplete=1, inverse: Incomplete=False) -> None:
        """Parameters
----------
a : float, optional
    The base of the exponential, i.e. the :math:`a` in :math:`Ca^{bx}`.
b : float, optional
    The scale for the exponent, i.e. the :math:`b` in :math:`Ca^{bx}`.
c : float, optional
    The coefficient of the exponential, i.e. the :math:`C` in :math:`Ca^{bx}`.
inverse : bool, optional
    If ``True``, the "forward" direction performs the inverse operation.

See also
--------
ultraplot.constructor.Scale"""
        ...

    def limit_range_for_scale(self, vmin: Incomplete, vmax: Incomplete, minpos: Incomplete) -> Incomplete:
        """Return the range *vmin* and *vmax* limited to positive numbers."""
        ...

class ExpTransform(mtransforms.Transform):
    input_dims = 1
    output_dims = 1
    has_inverse = True
    is_separable = True

    def __init__(self, a: Incomplete, b: Incomplete, c: Incomplete) -> None:
        """Parameters
----------
shorthand_name : str
    A string representing the "name" of the transform. The name carries
    no significance other than to improve the readability of
    ``str(transform)`` when DEBUG=True."""
        ...

    def inverted(self) -> Incomplete:
        """Return the corresponding inverse transformation.

It holds ``x == self.inverted().transform(self.transform(x))``.

The return value of this method should be treated as
temporary.  An update to *self* does not cause a corresponding
update to its inverted copy."""
        ...

    def transform_non_affine(self, a: Incomplete) -> Incomplete:
        """Apply only the non-affine part of this transformation.

``transform(values)`` is always equivalent to
``transform_affine(transform_non_affine(values))``.

In non-affine transformations, this is generally equivalent to
``transform(values)``.  In affine transformations, this is
always a no-op.

Parameters
----------
values : array
    The input values as an array of length `input_dims` or
    shape (N, `input_dims`).

Returns
-------
array
    The output values as an array of length `output_dims` or
    shape (N, `output_dims`), depending on the input."""
        ...

class InvertedExpTransform(mtransforms.Transform):
    input_dims = 1
    output_dims = 1
    has_inverse = True
    is_separable = True

    def __init__(self, a: Incomplete, b: Incomplete, c: Incomplete) -> None:
        """Parameters
----------
shorthand_name : str
    A string representing the "name" of the transform. The name carries
    no significance other than to improve the readability of
    ``str(transform)`` when DEBUG=True."""
        ...

    def inverted(self) -> Incomplete:
        """Return the corresponding inverse transformation.

It holds ``x == self.inverted().transform(self.transform(x))``.

The return value of this method should be treated as
temporary.  An update to *self* does not cause a corresponding
update to its inverted copy."""
        ...

    def transform_non_affine(self, a: Incomplete) -> Incomplete:
        """Apply only the non-affine part of this transformation.

``transform(values)`` is always equivalent to
``transform_affine(transform_non_affine(values))``.

In non-affine transformations, this is generally equivalent to
``transform(values)``.  In affine transformations, this is
always a no-op.

Parameters
----------
values : array
    The input values as an array of length `input_dims` or
    shape (N, `input_dims`).

Returns
-------
array
    The output values as an array of length `output_dims` or
    shape (N, `output_dims`), depending on the input."""
        ...

class MercatorLatitudeScale(_Scale, mscale.ScaleBase):
    """Axis scale that is linear in the [Mercator projection latitude](http://en.wikipedia.org/wiki/Mercator_projection). Adapted from [this example](https://matplotlib.org/2.0.2/examples/api/custom_scale_example.html).
The scale function is as follows:

.. math::

    y = \\ln(\\tan(\\pi x \\,/\\, 180) + \\sec(\\pi x \\,/\\, 180))

The inverse scale function is as follows:

.. math::

    x = 180\\,\\arctan(\\sinh(y)) \\,/\\, \\pi"""
    name = 'mercator'

    def __init__(self, thresh: Incomplete=85.0) -> None:
        """Parameters
----------
thresh : float, optional
    Threshold between 0 and 90, used to constrain axis limits
    between ``-thresh`` and ``+thresh``.

See also
--------
ultraplot.constructor.Scale"""
        ...

    def limit_range_for_scale(self, vmin: Incomplete, vmax: Incomplete, minpos: Incomplete) -> Incomplete:
        """Return the range *vmin* and *vmax* limited to within +/-90 degrees
(exclusive)."""
        ...

class MercatorLatitudeTransform(mtransforms.Transform):
    input_dims = 1
    output_dims = 1
    is_separable = True
    has_inverse = True

    def __init__(self, thresh: Incomplete) -> None:
        """Parameters
----------
shorthand_name : str
    A string representing the "name" of the transform. The name carries
    no significance other than to improve the readability of
    ``str(transform)`` when DEBUG=True."""
        ...

    def inverted(self) -> Incomplete:
        """Return the corresponding inverse transformation.

It holds ``x == self.inverted().transform(self.transform(x))``.

The return value of this method should be treated as
temporary.  An update to *self* does not cause a corresponding
update to its inverted copy."""
        ...

    def transform_non_affine(self, a: Incomplete) -> Incomplete:
        """Apply only the non-affine part of this transformation.

``transform(values)`` is always equivalent to
``transform_affine(transform_non_affine(values))``.

In non-affine transformations, this is generally equivalent to
``transform(values)``.  In affine transformations, this is
always a no-op.

Parameters
----------
values : array
    The input values as an array of length `input_dims` or
    shape (N, `input_dims`).

Returns
-------
array
    The output values as an array of length `output_dims` or
    shape (N, `output_dims`), depending on the input."""
        ...

class InvertedMercatorLatitudeTransform(mtransforms.Transform):
    input_dims = 1
    output_dims = 1
    is_separable = True
    has_inverse = True

    def __init__(self, thresh: Incomplete) -> None:
        """Parameters
----------
shorthand_name : str
    A string representing the "name" of the transform. The name carries
    no significance other than to improve the readability of
    ``str(transform)`` when DEBUG=True."""
        ...

    def inverted(self) -> Incomplete:
        """Return the corresponding inverse transformation.

It holds ``x == self.inverted().transform(self.transform(x))``.

The return value of this method should be treated as
temporary.  An update to *self* does not cause a corresponding
update to its inverted copy."""
        ...

    def transform_non_affine(self, a: Incomplete) -> Incomplete:
        """Apply only the non-affine part of this transformation.

``transform(values)`` is always equivalent to
``transform_affine(transform_non_affine(values))``.

In non-affine transformations, this is generally equivalent to
``transform(values)``.  In affine transformations, this is
always a no-op.

Parameters
----------
values : array
    The input values as an array of length `input_dims` or
    shape (N, `input_dims`).

Returns
-------
array
    The output values as an array of length `output_dims` or
    shape (N, `output_dims`), depending on the input."""
        ...

class SineLatitudeScale(_Scale, mscale.ScaleBase):
    """Axis scale that is linear in the sine transformation of *x*. The axis
limits are constrained to fall between ``-90`` and ``+90`` degrees.
The scale function is as follows:

.. math::

    y = \\sin(\\pi x/180)

The inverse scale function is as follows:

.. math::

    x = 180\\arcsin(y)/\\pi"""
    name = 'sine'

    def __init__(self) -> None:
        """See also
--------
ultraplot.constructor.Scale"""
        ...

    def limit_range_for_scale(self, vmin: Incomplete, vmax: Incomplete, minpos: Incomplete) -> tuple[int, int]:
        """Return the range *vmin* and *vmax* limited to within +/-90 degrees
(inclusive)."""
        ...

class SineLatitudeTransform(mtransforms.Transform):
    input_dims = 1
    output_dims = 1
    is_separable = True
    has_inverse = True

    def __init__(self) -> None:
        """Parameters
----------
shorthand_name : str
    A string representing the "name" of the transform. The name carries
    no significance other than to improve the readability of
    ``str(transform)`` when DEBUG=True."""
        ...

    def inverted(self) -> Incomplete:
        """Return the corresponding inverse transformation.

It holds ``x == self.inverted().transform(self.transform(x))``.

The return value of this method should be treated as
temporary.  An update to *self* does not cause a corresponding
update to its inverted copy."""
        ...

    def transform_non_affine(self, a: Incomplete) -> Incomplete:
        """Apply only the non-affine part of this transformation.

``transform(values)`` is always equivalent to
``transform_affine(transform_non_affine(values))``.

In non-affine transformations, this is generally equivalent to
``transform(values)``.  In affine transformations, this is
always a no-op.

Parameters
----------
values : array
    The input values as an array of length `input_dims` or
    shape (N, `input_dims`).

Returns
-------
array
    The output values as an array of length `output_dims` or
    shape (N, `output_dims`), depending on the input."""
        ...

class InvertedSineLatitudeTransform(mtransforms.Transform):
    input_dims = 1
    output_dims = 1
    is_separable = True
    has_inverse = True

    def __init__(self) -> None:
        """Parameters
----------
shorthand_name : str
    A string representing the "name" of the transform. The name carries
    no significance other than to improve the readability of
    ``str(transform)`` when DEBUG=True."""
        ...

    def inverted(self) -> Incomplete:
        """Return the corresponding inverse transformation.

It holds ``x == self.inverted().transform(self.transform(x))``.

The return value of this method should be treated as
temporary.  An update to *self* does not cause a corresponding
update to its inverted copy."""
        ...

    def transform_non_affine(self, a: Incomplete) -> Incomplete:
        """Apply only the non-affine part of this transformation.

``transform(values)`` is always equivalent to
``transform_affine(transform_non_affine(values))``.

In non-affine transformations, this is generally equivalent to
``transform(values)``.  In affine transformations, this is
always a no-op.

Parameters
----------
values : array
    The input values as an array of length `input_dims` or
    shape (N, `input_dims`).

Returns
-------
array
    The output values as an array of length `output_dims` or
    shape (N, `output_dims`), depending on the input."""
        ...

class CutoffScale(_Scale, mscale.ScaleBase):
    """Axis scale composed of arbitrary piecewise linear transformations.
The axis can undergo discrete jumps, "accelerations", or "decelerations"
between successive thresholds."""
    name = 'cutoff'

    def __init__(self, *args: Incomplete) -> None:
        """Parameters
----------
*args : thresh_1, scale_1, ..., thresh_N, [scale_N], optional
    Sequence of "thresholds" and "scales". If the final scale is
    omitted (i.e. you passed an odd number of arguments) it is set
    to ``1``. Each ``scale_i`` in the sequence can be interpreted
    as follows:

    * If ``scale_i < 1``, the axis is decelerated from ``thresh_i`` to
      ``thresh_i+1``. For ``scale_N``, the axis is decelerated
      everywhere above ``thresh_N``.
    * If ``scale_i > 1``, the axis is accelerated from ``thresh_i`` to
      ``thresh_i+1``. For ``scale_N``, the axis is accelerated
      everywhere above ``thresh_N``.
    * If ``scale_i == numpy.inf``, the axis *discretely jumps* from
      ``thresh_i`` to ``thresh_i+1``. The final scale ``scale_N``
      *cannot* be ``numpy.inf``.

See also
--------
ultraplot.constructor.Scale

Example
-------
>>> import ultraplot as uplt
>>> import numpy as np
>>> scale = uplt.CutoffScale(10, 0.5)  # move slower above 10
>>> scale = uplt.CutoffScale(10, 2, 20)  # move faster between 10 and 20
>>> scale = uplt.CutoffScale(10, np.inf, 20)  # jump from 10 to 20"""
        ...

class CutoffTransform(mtransforms.Transform):
    input_dims = 1
    output_dims = 1
    has_inverse = True
    is_separable = True

    def __init__(self, threshs: Incomplete, scales: Incomplete, zero_dists: Incomplete=None) -> None:
        """Parameters
----------
shorthand_name : str
    A string representing the "name" of the transform. The name carries
    no significance other than to improve the readability of
    ``str(transform)`` when DEBUG=True."""
        ...

    def inverted(self) -> Incomplete:
        """Return the corresponding inverse transformation.

It holds ``x == self.inverted().transform(self.transform(x))``.

The return value of this method should be treated as
temporary.  An update to *self* does not cause a corresponding
update to its inverted copy."""
        ...

    def transform_non_affine(self, a: Incomplete) -> Incomplete:
        """Apply only the non-affine part of this transformation.

``transform(values)`` is always equivalent to
``transform_affine(transform_non_affine(values))``.

In non-affine transformations, this is generally equivalent to
``transform(values)``.  In affine transformations, this is
always a no-op.

Parameters
----------
values : array
    The input values as an array of length `input_dims` or
    shape (N, `input_dims`).

Returns
-------
array
    The output values as an array of length `output_dims` or
    shape (N, `output_dims`), depending on the input."""
        ...

class InverseScale(_Scale, mscale.ScaleBase):
    """Axis scale that is linear in the *inverse* of *x*. The forward and inverse
scale functions are as follows:

.. math::

    y = x^{-1}"""
    name = 'inverse'

    def __init__(self) -> None:
        """See also
--------
ultraplot.constructor.Scale"""
        ...

    def limit_range_for_scale(self, vmin: Incomplete, vmax: Incomplete, minpos: Incomplete) -> Incomplete:
        """Return the range *vmin* and *vmax* limited to positive numbers."""
        ...

class InverseTransform(mtransforms.Transform):
    input_dims = 1
    output_dims = 1
    is_separable = True
    has_inverse = True

    def __init__(self) -> None:
        """Parameters
----------
shorthand_name : str
    A string representing the "name" of the transform. The name carries
    no significance other than to improve the readability of
    ``str(transform)`` when DEBUG=True."""
        ...

    def inverted(self) -> Incomplete:
        """Return the corresponding inverse transformation.

It holds ``x == self.inverted().transform(self.transform(x))``.

The return value of this method should be treated as
temporary.  An update to *self* does not cause a corresponding
update to its inverted copy."""
        ...

    def transform_non_affine(self, a: Incomplete) -> Incomplete:
        """Apply only the non-affine part of this transformation.

``transform(values)`` is always equivalent to
``transform_affine(transform_non_affine(values))``.

In non-affine transformations, this is generally equivalent to
``transform(values)``.  In affine transformations, this is
always a no-op.

Parameters
----------
values : array
    The input values as an array of length `input_dims` or
    shape (N, `input_dims`).

Returns
-------
array
    The output values as an array of length `output_dims` or
    shape (N, `output_dims`), depending on the input."""
        ...

def _scale_factory(scale: Incomplete, axis: Incomplete, *args: Incomplete, **kwargs: Incomplete) -> Incomplete:
    """Generate an axis scale.

Parameters
----------
scale : str or [ScaleBase](https://matplotlib.org/stable/api/_as_gen/matplotlib.scale.ScaleBase.html)
    The axis scale name or scale instance.
axis : [Axis](https://matplotlib.org/stable/api/_as_gen/matplotlib.axis.Axis.html)
    The axis instance.
*args, **kwargs
    Passed to [ScaleBase](https://matplotlib.org/stable/api/_as_gen/matplotlib.scale.ScaleBase.html) if `scale` is a string."""
    ...
